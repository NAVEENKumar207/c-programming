#include<stdio.h>
void main() {
    int n;
    printf("Enter the size of the array: ");
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++) {
    printf("Enter the %dth elements of the array: ",i);
        scanf("%d", &arr[i]);
    }
    
    int largest = arr[0];
    for (int i = 1; i < n; i++) {
        if (arr[i] > largest) {
            largest = arr[i];
        }
    }
    printf("\nThe largest element in the array is: %d\n", largest);
}
--------------------------------------------------------------------
Output :
Enter the number of the elements in the array: 5
Enter the 0th elements of the array: 25
Enter the 1th elements of the array: 68
Enter the 2th elements of the array: 95
Enter the 3th elements of the array: 55
Enter the 4th elements of the array: 36

The largest element in the array is: 95
